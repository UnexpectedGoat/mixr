<div class="container">
    <div class="row">
        <div class="col s-12">
            <ul class="collection">
                {{#each pantry}}
                    <li class="collection-item">{{ingredient}}</li>
                {{/each}}
            </ul>
        </div>
    </div>
</div> 
 