<div class="container">
    <div class="row">
        <div class="col s-12">
            <ul class="collection">
                {{#each pantry}}
                    <li class="collection-item">
                        {{ingredient}} 
                        <a><i class="material-icons right">delete</i></a>
                    </li>
                {{/each}}
            </ul>
        </div>
    </div>
</div> 
 